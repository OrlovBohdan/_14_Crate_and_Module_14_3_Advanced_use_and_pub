#[test]

/*

// FILL in the blank in two ways
// DON'T add new code line
use std::collections::__;

fn main() {
    let _c1:HashMap<&str, i32> = HashMap::new();
    let mut c2 = BTreeMap::new();
    c2.insert(1, "a");
    let _c3: HashSet<i32> = HashSet::new();
}
*/

fn main() {
    let _c1: HashMap<&str, i32> = HashMap::new();
    let mut c2 = BTreeMap::new();
    c2.insert(1, "a");
    let _c3: HashSet<i32> = HashSet::new();
}


use std::collections::{HashMap, BTreeMap, HashSet};

/*
use std::collections::{HashMap, BTreeMap, HashSet}; - Це імплементує всі три колекції, що використовуються в main().
HashMap - використовується для зберігання пар ключ-значення, де ключі не зберігаються в порядку.
BTreeMap - також використовується для пар ключ-значення, але зберігає ключі в упорядкованому вигляді.
HashSet - використовується для зберігання унікальних значень без пар ключ-значення.
*/